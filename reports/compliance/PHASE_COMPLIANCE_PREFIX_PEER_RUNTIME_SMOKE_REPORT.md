# PHASE — Compliance Prefix/Peer Runtime Smoke

**Date:** 2026-05-26T03:12:37.174Z
**Verdict:** **GO**
**Device ID:** 1
**Job ID:** 63
**Job status:** failed (pass=1, fail=78)

## 1. Commits validados

```
8332969 fix(compliance): resolve BGP peer dependencies by address-family context
1d982e8 fix(compliance): resolve IPv4 and IPv6 route-policy prefix dependencies
11ba89b fix(l2): classify dot1q L3 subinterfaces and isolate findings
0e30b68 fix(l2): add idempotent schema hotfix for live circuits table
897408c docs(l2): add MVP validation report and fix circuit insert
```

- HEAD: `8332969`
- 8332969 ancestor: yes
- Target fixes: `1d982e8` (prefix IPv4/IPv6), `8332969` (BGP peer AF context)

## 2. Rebuild API

- Command: `docker compose up -d --build api`
- Container: `netops-api`
- Health: GET `/api/healthz` → ok

## 3. Endpoints / UI

| Check | Result |
|-------|--------|
| POST `/api/compliance/jobs` (bgp) | ok |
| GET job + JSON report | ok |
| GET `/api/compliance-findings?jobId=` filter | ok |
| UI Compliance `http://127.0.0.1:3005/compliance` | ok — page, findings, filters, groups |

## 4. Findings summary (job 63)

- Total findings: 140
- Fail: 78
- Pass: 1
- Unknown: 0

## 5. IPv6 prefix false positives

Expected: no fail treating IPv6 prefix-list as `ip-prefix`.

| Check | Hits |
|-------|------|
| GATEWAY-IPV6 as ip-prefix | 0 |
| AS266208-4WNET-V6-332 as ip-prefix | 0 |
| C17-BLOCKLIST-IPV6 as ip-prefix | 0 |
| C17-PREFIX-PREFERENCE-IPV6 as ip-prefix | 0 |
| C17-IMPORT-IPV6 node 3011 | 0 |
| MALHA-MNS-Export-IPv6 node 10 | 0 |

No IPv6 false positives detected.


## 6. IPv4 prefix false positives

| Check | Hits |
|-------|------|
| AS268707-4WNET | 0 |
| AS268707-4WNET-MALHA | 0 |
| DEFAULT | 0 |
| GATEWAY-IPV4 | 0 |
| C17-PREFIX-PREFERENCE-IPV4 | 0 |

## 7. BGP peer root/family (172.28.1.138 / WIFIZAO)

- Peer/policy false-positive fails: 0
- 172.28.1.138 without spurious missing: yes
- Explicit pass row for peer: n/a

## 8. Peer-group (IX-AM / IX-RR / MALHA)

| Check | Hits |
|-------|------|
| IX-AM peer inherited policy | 0 |
| peer-group inherited policy | 0 |
| MALHA group missing | 0 |

## 9. Real errors preserved

Sample remaining fail findings (not in FP list):

- Peer 10.20.1.1 state=Unknown.
- Peer provider 10.20.1.1 sem export policy.
- Peer 10.20.1.5 state=Unknown.
- Peer provider 10.20.1.5 sem export policy.
- Peer 10.20.255.2 state=Unknown.
- Cliente 10.20.255.2 sem import policy.
- Peer 10.20.255.5 state=Unknown.
- Cliente 10.20.255.5 sem import policy.

## 10. Parser selftests (offline, no discovery)

- `compliance-prefix-route-policy-selftest.mjs`: PASS
- `compliance-ipv6-prefix-route-policy-selftest.mjs`: PASS
- `bgp-peer-dependency-selftest.mjs`: PASS
- `policy-dependency-catalog-pipeline-selftest.mjs`: PASS

## 12. UI smoke

- Tela Compliance abre e lista findings agrupados
- Filtros (status/context/severity comboboxes, Actionable Only) presentes
- Busca na página: `referencia ip-prefix GATEWAY-IPV6` → **0 matches**
- Busca: `GATEWAY-IPV6`, `WIFIZAO` → **0 matches** (sem FP visível na UI atual)
- Evidence em detalhe: JSON sanitizado; `dependencyType` em fails de policy viria como `ipv6-prefix` / `ip-prefix` / `route-policy` (nenhum fail dependency neste job)

## 13. Logs / safety (api container, last 15m)

| Rule | Violation |
|------|-----------|
| SSH activity | no |
| Discovery | no |
| Secrets in logs | no |
| Critical runtime | no |

## 12. dependencyType / messaging

- IPv6 findings with metadata `ipv6-prefix`: 0 (spot-check)
- UI must show `ipv6-prefix` not `ip-prefix` for IPv6 deps — verify on Compliance detail for GATEWAY-IPV6 if any pass/unknown row exists

## GO / NO-GO

**GO** — Falsos positivos prefix/peer dependency eliminados no runtime (job ${jobId}); erros operacionais reais mantidos; selftests green; sem SSH/discovery.

### Pendência (não commitada nesta fase)

- `buildPolicyDependencyConfigFromSnapshot(snapshot, { rawConfig })` em working tree — necessário para runtime com snapshot DB antigo; recomendado commit follow-up.

---

*Generated by `tools/compliance-runtime-smoke.mjs`. No commit. No SSH. No discovery.*
